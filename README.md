
## opentraces moved

`opentraces` now lives in its own tap. Install it with:

```bash
brew install JayFarei/opentraces/opentraces
```

This generic tap retains other formulae (e.g. `lazymem`). If you hit
`Error: Formulae found in multiple taps`, run `brew untap jayfarei/tap`.
